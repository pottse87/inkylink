/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Keep default runtime; individual routes can set `export const runtime = "nodejs"`
  // Add other options here if you need them later (images, rewrites, etc.)
  // output: "standalone",   // uncomment if you want standalone output
};
export default nextConfig;
